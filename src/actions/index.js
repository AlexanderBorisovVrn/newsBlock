import {fetchNews, fetchArticle} from './fetchDataActions'
import {onForm, hideForm, onPasswordVisible, onLoggedIn} from './onLoggedActions';
import {setCategory, setDateFrom, setDateTo, sortBy} from './configFetchData';

export {
  setCategory,
  setDateFrom,
  setDateTo,
  sortBy,
  fetchNews,
  fetchArticle,
  onForm,
  hideForm,
  onPasswordVisible,
  onLoggedIn
};